# 验证计划

> 真实项目请在此记录仿真验证策略和覆盖率目标。

## 验证范围

`<哪些模块/功能需要仿真验证>`

## 验证环境

- **仿真器**：`<xsim / ModelSim / Verilator / 其他>`
- **语言**：`<Verilog / SystemVerilog / UVM>`

## 测试用例

| 用例 ID | 目标模块 | 描述 | 类型 | 状态 |
|---------|---------|------|------|------|
| `<TC-001>` | `<module>` | `<描述>` | `<directed/random>` | `<planned/pass/fail>` |

## 覆盖率目标

| 类型 | 目标 |
|------|------|
| 代码覆盖率 | `<目标百分比>` |
| 功能覆盖率 | `<功能点列表>` |

## 回归策略

`<回归测试频率和范围>`
