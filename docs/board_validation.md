# 上板验证

> 真实项目请在此记录上板验证策略和每次验证结果索引。

## 目标板卡

- **型号**：`<FPGA 开发板型号>`
- **器件**：`<FPGA 器件型号>`

## 验证清单

| 项 | 描述 | 预期结果 | 状态 |
|----|------|---------|------|
| JTAG 检测 | FPGA 是否可被 JTAG 检测 | 检测成功 | `<pending>` |
| 基本 I/O | LED/按键等基本外设 | 正常工作 | `<pending>` |
| 时钟 | 各时钟域频率确认 | 频率正确 | `<pending>` |
| `<功能 A>` | `<功能 A 的上板验证>` | `<预期>` | `<pending>` |

## 每次运行记录

| Run ID | Date | Bitstream | Result | Notes |
|--------|------|-----------|--------|-------|
| `<RUN-001>` | `<YYYY-MM-DD>` | `<路径>` | `<pass/fail>` | `<备注>` |

## ILA 配置

`<ILA 核的配置和触发条件>`

## 自动化脚本

`<PS 端或上位机脚本说明>`
