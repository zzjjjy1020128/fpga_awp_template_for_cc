# 架构文档

> 真实项目请在此记录 FPGA 设计的顶层架构。

## 设计目标

`<项目的设计目标>`

## 系统框图

```text
<ASCII 框图或引用图片路径>
```

## 模块划分

| 模块名 | 功能 | 接口 | 状态 |
|--------|------|------|------|
| `<module_a>` | `<功能>` | `<接口协议>` | `<planned/implemented/verified>` |

## 接口规范

`<顶层接口定义，包括 AXI/AXI-Lite/AXI-Stream 等>`

## 时钟域

| 时钟域 | 频率 | 来源 | 用途 |
|--------|------|------|------|
| `<clk_a>` | `<频率>` | `<来源>` | `<用途>` |

## 复位策略

`<复位方案描述>`

## CDC 处理

`<跨时钟域信号列表和处理方式>`
