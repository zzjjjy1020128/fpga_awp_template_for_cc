---
handoff_id: "HO-E001-OR-006-001"
from_session: "SESS-E001-OR-006"
date: "2026-06-09"
status: "resolved"
---

# Handoff: Cross-trigger L0-L4 done, DMA数据通路诊断完成, 加速器capture_done待修复

## Session 概览

- **Handoff ID**: HO-E001-OR-006-001
- **Session ID**: SESS-E001-OR-006
- **Date**: 2026-06-09
- **原因**: Session 结束, TASK-E001-027 done (L0-L4), TASK-E001-024 根因定位但未修复

## Gate Status

| Task ID | Target | L0 | L1a | L1b | L1c | L2 | L3 | L4 | L5 | L6 | L7 |
|---------|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| TASK-E001-027 | L0 | pass | pending | pending | pending | pending | pending | pending | pending | pending | skip |
| TASK-E001-024 | L6 | skip | skip | skip | skip | skip | skip | skip | pass | pending | pending |

## 已完成

| Task ID | 产出 | 状态 |
|---------|------|:--:|
| TASK-E001-027 | RTL cross-trigger + ILA 例化修复 + L0审查 + L2-L4 Vivado验证 | done |
| ISS-E001-008 | System ILA BASIC trigger 澄清 (实测可用, 不需要RTL ILA) | resolved |

## 关键发现

1. **DMA + HP0 数据通路正常**：ILA 波形确认 MM2S 通过 HP0 从 DDR 正确读出 1024 字节增量数据
2. **根因在加速器 axis_input**：数据完整到达 s_axis (tvalid=1, tready=1, tdata递增), 但 capture_done 不触发, FSM 卡在 CAPTURE
3. **AFI 寄存器是死胡同**：Zynq-7000 上只读, ps7_init 不配是设计行为, 非 DMA 不通的根因
4. **System ILA 支持 BASIC_ONLY 触发**：运行时可通过 `TRIGGER_COMPARE_VALUE {eq1'b1}` 设置

## 关键资产

```
rtl/dbg_trigger_hub.sv                    ← PL anchor event → ILA trigger hub
rtl/axil_2d_shift.sv (modified)            ← probe1 位宽修复 (32bit)
board/ps_dma_test/src/dma_gated.c          ← 软件gate + XAxiDma驱动 (推荐模板)
board/ps_dma_test/src/hp0_test.c           ← 零依赖裸机DMA测试
board/ps_dma_test/src/dma_xaxidma_test.c   ← XAxiDma标准测试 (中断+SimpleTransfer)
board/create_bsp.tcl                       ← HSI一键BSP生成
board/vitis_bsp/                           ← 从新XSA生成的完整BSP (libxil.a + headers)
board/run_vitis_test.tcl                   ← 标准Vitis下载流程
board/minimal_xsct.tcl                     ← SDK等价最小流程 (rst -system → ps7_init → fpga -f → dow → con)
board/gated_ila_flow.tcl                   ← gate + ILA同步捕获流程
board/release_gate.tcl                     ← mwr gate释放 + con
vivado/.../design_1_wrapper.xsa            ← 用户新导出的XSA (含bitstream)
.awp/runs/ila1_triggered.csv              ← ILA捕获数据 (511拍有效stream)
.claude/skills/zynq-debug-toolchain/SKILL.md  ← 完全重写的方法论
.awp/orchestration_guide.md                ← 新增工具链边界检查章节
```

## 已知阻塞

1. **加速器 capture_done (ISS-E001-009)**：axis_input 收到全部数据但不触发帧完成信号
   - 可能原因: C代码写的 IMG_ROWS/IMG_COLS 寄存器地址与 RTL decoder 不匹配
   - 修复方案: rtl_implementer 检查 regs_top/axil_slave_if 寄存器映射, 或直接对 axis_input 加 ILA probe 看内部计数

## 下一步行动

1. **修复加速器 capture_done**：创建新 task (rtl_implementer), 检查 axis_input 像素计数逻辑 vs C代码寄存器写入地址
2. **继续 TASK-E001-024**：capture_done 修复后重新跑 DMA gate test + ILA 验证输出侧
3. **L6 通过后推进 TASK-E001-026 (L7 复盘)**

## 工具链边界检查（下一 session 启动时必读）

> 已在 `.awp/orchestration_guide.md` 开头和 `.claude/skills/zynq-debug-toolchain/SKILL.md` 中固化。
> 关键规则:
> 1. 进入 Vitis 前必须确认 BD 完整性 (ILA probe 无悬空, stream 连接无断点)
> 2. XSA 必须与 Vivado 工程同步 (用户声明"已重新导出"=旧XSA作废)
> 3. 永不用 Vivado Hardware Manager 烧录 FPGA (覆盖 PS 状态)
> 4. ILA 触发条件设 handshake (tvalid=eq1'b1 AND tready=eq1'b1), 不用 don't-care
