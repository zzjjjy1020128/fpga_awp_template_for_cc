# Project Charter

> 项目章程 —— 定义项目的目标、范围、约束和成功标准。

## 项目概述

- **项目名称**：`<project_name>`
- **项目目标**：`<一句话描述项目要做什么>`
- **目标平台**：`<FPGA 器件型号，例如 Zynq UltraScale+ ZCU19EG>`
- **开发工具链**：`<Vivado 版本，仿真器，其他工具>`

## 范围

### 范围内
- `<模块/功能 A>`
- `<模块/功能 B>`

### 范围外
- `<明确不包含的内容>`

## 关键约束

- `<约束 1：例如时钟频率>`
- `<约束 2：例如资源上限>`
- `<约束 3：例如接口协议要求>`

## 验证目标

| 级别 | 描述 | 目标 |
|------|------|------|
| L0 | 静态审查 | `<是否要求>` |
| L1a | 模块级单元仿真（单模块，单帧/单事务） | `<是否要求>` |
| L1b | 数据通路闭环仿真（≥2 个数据通路模块串联，含跨帧测试） | `<是否要求>` |
| L1c | 全系统集成仿真（完整系统，所有接口，多帧/多事务） | `<是否要求>` |
| L2 | 综合 | `<是否要求>` |
| L3 | 实现与时序 | `<是否要求>` |
| L4 | 比特流生成 | `<是否要求>` |
| L5 | 板上冒烟测试 | `<是否要求>` |
| L6 | 板上数据正确性 | `<是否要求>` |
| L7 | 性能/资源复盘 | `<是否要求>` |

## 成功标准

- `<标准 1>`
- `<标准 2>`

## 风险

- `<风险 1：描述和缓解措施>`
- `<风险 2：描述和缓解措施>`
